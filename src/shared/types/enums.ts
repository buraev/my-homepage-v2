export enum Indexes {
  all = "all",
  catalog = "services",
  news = "news",
  government = "institutions",
  instructions = "instructions",
  home = "home",
  esh = "esh",
  custom = "custom",
}

export enum SortType {
  ASC = "asc",
  DESC = "desc",
}
